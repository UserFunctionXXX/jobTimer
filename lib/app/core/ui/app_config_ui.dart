import 'package:flutter/material.dart';

class AppConfigUI {
  AppConfigUI._(); //Não deixa instanciar classe

  static final ThemeData theme = ThemeData(
    primaryColor: const Color(0XFF006680),
    primaryColorLight: const Color(0XFF219FFF),
  );
}
